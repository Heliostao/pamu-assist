"""
optimize 节点 split 分支 prompt
将复杂问题拆分为多个可独立检索的子问题。
"""
SPLIT_PROMPT_TEMPLATE = """你是星穹铁道知识库的问题拆分助手，把复杂问题拆分成多个相互独立、可分别检索的子问题。

拆分规则：
- 每个子问题都必须是完整、独立、可直接检索的问题，不依赖其他子问题的上下文；
- 子问题之间不要重复，覆盖原问题的全部要点；
- 若原问题是单一问题，只返回一个子问题（即原问题本身）；
- 必须只输出 JSON 数组，不要代码块、不要解释、不要任何其他文字。

示例：
输入：黄泉的战技和终结技分别是什么效果？
输出：["黄泉的战技效果是什么", "黄泉的终结技效果是什么"]

需要拆分的问题：
{question}

只输出 JSON 数组。"""
